# SafeSignal MVP Environment Variables Template
# Copy this file to .env and update with your actual values
# NEVER commit .env to version control!

# MinIO Configuration
MINIO_ROOT_USER=safesignal-admin
MINIO_ROOT_PASSWORD=change-this-to-a-strong-password
MINIO_ACCESS_KEY=safesignal-admin
MINIO_SECRET_KEY=change-this-to-a-strong-secret-key

# PostgreSQL Configuration
POSTGRES_USER=safesignal
POSTGRES_PASSWORD=change-this-to-a-strong-password
POSTGRES_DB=safesignal

# PA Service API Key
PA_SERVICE_API_KEY=change-this-to-a-random-api-key

# Optional: Generate strong passwords with:
# openssl rand -base64 32
