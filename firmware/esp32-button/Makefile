#
# SafeSignal ESP32-S3 Button Firmware
# Main Makefile for ESP-IDF (Legacy Make)
#

PROJECT_NAME := safesignal-button

# Partition table
PARTITION_TABLE_CSV_PATH := $(PROJECT_PATH)/partitions.csv

# Bootloader config
BOOTLOADER_OFFSET := 0x1000

# Extra component directories
EXTRA_COMPONENT_DIRS :=

include $(IDF_PATH)/make/project.mk
